[project]
name = "stress-happy-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "soobin4753", email = "soobin4753@naver.com" }
]
requires-python = ">=3.12"
dependencies = [
    "koreanize-matplotlib>=0.1.1",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
    "setuptools>=84.0.0",
    "streamlit>=1.61.1",
]

[project.scripts]
stress-happy-analysis = "stress_happy_analysis:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false